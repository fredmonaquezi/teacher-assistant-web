# Running Records (dormant)

Running Records is intentionally outside the active Teacher Assistant routes and workspace data graph.
Its UI, UI test, translations, and feature-specific data loader are preserved together in this directory.

To restore the feature, add a lazy route and a feature-owned data/actions hook. Do not reconnect it to the
active subjects or Activity Assessment domains.
